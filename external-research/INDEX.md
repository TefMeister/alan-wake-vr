# Research index

**Last `/gr` pass: 2026-09-05 (estate sweep) — CHECK-IN** (board OPEN block re-read after today's `/lm`; INDEX + one new topic; one search)**.** Inbox empty. **Today's headline is in-house, not mine: the handedness is answered live — LEFT, `clip.w = +view.z` — so the stereo build is unblocked and no public question stands in front of it.** The one row with a public angle was the layered-hook race that killed launch 1. Searched: the Steam-overlay/proxy-DLL conflict is **well documented as a class** — the newer overlay hooking code fights other hooks and proxies, with `CreateDevice` in its init path — which supports the row's diagnosis but is community material naming neither this game nor this recursion, and **changes nothing about the fix**. The useful part is cheaper than a finding: **disabling the Steam overlay per-game should make the foreign pointer not appear at all**, turning one ambiguous relaunch into two readable ones.

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep, third pass today) — CHECK-IN** (verdict tracking only; INDEX + one topic)**.** Inbox empty, nothing searched. **This morning's lead is confirmed and its row is flipped 🆕 → ✅:** `/pd` built the `FreeLibrary` fix and an `/lm` launch verified it live — the proxy now owns the real render device. Recorded from the modding lane's notes rather than a verdict drop, so an empty inbox here does not mean nothing happened._

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep, second pass today) — CHECK-IN** (inbox drained — one `/gs` hygiene drop; one topic header)**.** **Inbox drained, nothing searched.** The `d3dcompiler` topic led with "🆕 new · ⭐ high" and an unqualified build-the-proxy recommendation 85 lines above its own withdrawal, so a reader opening the file directly met the dead recommendation first; its header now states the disproof and points at the Withdrawn section. The INDEX row was already correct._

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep) — CHECK-IN, targeted** (board OPEN block, INDEX, dossier §3/§4, the proxy source and the recon log; four searches on the one `[PD]` row)**.** Inbox empty. **One new topic that answers the `[PD]` "where does the real `IDirect3D9` come from?" row outright:** the game probes `d3d9.dll` and unloads it 6 ms after `Direct3DCreate9`, then loads it again for the renderer — and our proxy never `FreeLibrary`s the system `d3d9.dll` it loaded by full path, so Windows hands the second `LoadLibrary("d3d9.dll")` the resident system copy by base name (Microsoft's own `LoadLibrary` rule) and the proxy is bypassed. **ReShade needed exactly this fix for exactly this game** (commit `74347b91d`, 4.5.2, *"necessary for Alan Wake to work"*). One line in `DLL_PROCESS_DETACH`; then re-test the "CONFIRMED BROKEN" `install_createdevice_hook`, which was judged against the probe object. Drop to `engine-research/inbox/`; the loader rule to `/sr`._
_Previous: **Last `/gr` pass: 2026-09-03 (estate sweep, second pass today) — CHECK-IN** (inbox drained — two `/pd` verdicts; INDEX; the two topics they answer)**.** **One lead confirmed, one withdrawn.** The FOV / time-scale byte patterns **both ported exactly** and led the modding side to the camera object behind ONE static global on a **no-ASLR** image — the most useful structural fact on this game so far (→ ✅). The `d3dcompiler` proxy topic is **`[disproved]`**: the shader bank ships pre-compiled with CTAB intact (9,971 tables) and the CTAB method answered §6 the same day; the game calls `D3DXCompileShaderFromFileA` and ships no source (→ ❌; the OpenAWE/AWTools pointers survive). One small pointer topic: 3D Vision is a discontinued driver feature since 425.31, so the native stereo hotkeys will not show a picture on a current driver — see the Alice topic it points at. Practice adopted at the modding side's request: **send byte patterns, not addresses.**
_Previous: **Last `/gr` pass: 2026-09-03 (estate sweep) — CHECK-IN** (board OPEN block + INDEX + the NVAPI topic; inbox held only my own three drops, still unread by the modding side, left in place)**.** No new topic — instead, **the board's own request was answered for the third time**: the first `[PD]` row still asks `/gr` to check `0x5E8F0BEC` against …_
_Previous: **Last `/gr` pass: 2026-09-02 (scoped re-run) — CHECK-IN** (board OPEN block + INDEX + the day's own drops; the dossier was read earlier today)**.** Inbox held only my own two drops, untouched by the modding side — left in place. One new topic, aimed at the bo…_
_2026-09-01 (second pass, estate sweep) — CHECK-IN:_ Inbox drained: `/pd`'s
request to confirm the NVAPI id→name mapping its `SetDriverMode` check rested on. **All six IDs
verified against NVIDIA's own published dispatch table**, so the retirement of the native-stereo
shortcut stands on a checked footing. One new topic; the reusable half (NVAPI is callable-by-ID and
the table is public, so “which NVAPI calls does this binary make?” is a static offline question for
any 3D-Vision-era game) went to the cross-engine library for `/sr`.
_Earlier the same day — FULL:_ inbox drained (the `-freecamera` verdict → ✅). Two new
topics, both answering questions the modding side asked directly: NVIDIA 3D Vision Automatic makes
its two eyes **in the driver**, so the game's stereo uniform corrects effects rather than moving a
camera — and `-forcestereo` is an **audio** switch, corrected into `engine-research/inbox/`. The
clip-space stereo footer this uncovered was filed to the cross-engine library for `/sr`.

Every research topic gathered for this project, newest first. Each row links to a self-contained
write-up in `topics/`. Status tags:

- 🆕 **new** — found, not yet acted on by the modding side.
- 👀 **reviewed** — a modding session has read it and factored it into a decision, but nothing shipped from it yet.
- ✅ **incorporated** — directly led to a real change (code, a test, a note) in one of the other five repos; linked below.
- ❌ **dead end** — checked out, didn't pan out; kept for the record so it isn't re-investigated from scratch.

| Date | Topic | Status | Summary |
| --- | --- | --- | --- |
| 2026-09-05 | [The layered-hook race is a known Steam-overlay conflict, and it can be switched off](topics/2026-09-05-the-layered-hook-race-is-a-known-steam-overlay-conflict-and-it-can-be-switched-off.md) | 🆕 new | Corroborates the `[PD]` row's "Steam overlay, most likely" and adds a free control for the `[FLAT]` relaunch behind it. Public record `[reported 2026-09-05]`: the older external overlay coexisted with `d3d9`/`dxgi`/`ddraw`/`opengl32` proxies, but **the newer overlay hooking code conflicts with other hooks and proxies**, with `CreateDevice` named in its init path and "two overlays hooking the same object will conflict" the recurring rule. ⚠️ Community threads, not a Valve statement, and **none names this game or this recursion** — it corroborates the *class*, and **nothing here changes the fix**, which rests on our own launch-1 measurement. ⭐ **The practical part: disable the Steam overlay per-game and the foreign pointer should not appear at all**, splitting one ambiguous run into two readable ones — overlay **off** gives the cleanest possible first stereo-proxy run (and if a foreign pointer *still* shows, it is **not** Steam, which would be a new finding); overlay **on** deliberately exercises the new refusal path. Record the overlay state beside every log. |
| 2026-09-04 | [The proxy is bypassed on reload because the system `d3d9.dll` is never freed](topics/2026-09-04-the-proxy-is-bypassed-on-reload-because-the-system-d3d9-is-never-freed.md) | ✅ incorporated | **✅ CONFIRMED LIVE THE SAME DAY.** `/pd` built the `FreeLibrary` fix and an `/lm` launch ran it: one PID, the proxy releases the system reference on detach, and the game's **second** `LoadLibraryA("d3d9.dll")` finds the proxy `[verified-live 2026-09-04, n=1 launch]`. The proxy is now in the chain of the device the game actually renders with — what M1 and M2 both needed. Both halves of the diagnosis held. **⭐ Answers the `[PD]` "where does the REAL device's `IDirect3D9` come from?" row.** The three logged launches are all one probe: load → `Direct3DCreate9` succeeds → unload **6 ms later**. The game then loads `d3d9.dll` again for the renderer — but `proxy.c` loaded `C:\Windows\system32\d3d9.dll` by full path and never `FreeLibrary`s it on detach, so it stays resident, and Microsoft's `LoadLibrary` rule ("searches for loaded modules whose base name matches … if the name matches, the load succeeds") hands the game the **system** copy. The game folder is never searched again; the proxy sees nothing; gameplay runs fine. **ReShade hit exactly this in exactly this game** — commit `74347b91d` (2019-12-19, 4.5.2 "Fixed hooking in Alan Wake"): *"Free reference to the module loaded for export hooks (this is necessary for Alan Wake to work)"*. Fix: one `FreeLibrary(real_d3d9)` in `DLL_PROCESS_DETACH`; expect a second "proxy loaded" block in the same PID, then `CreateDevice`. Then re-test `install_createdevice_hook`, whose "CONFIRMED BROKEN" verdict was reached against the probe object. |
| 2026-09-03 | [3D Vision on a current driver is a discontinued feature — pointer to the Alice write-up](topics/2026-09-03-3d-vision-on-a-current-driver-is-a-dead-feature-pointer-to-the-alice-topic.md) | 🆕 new | NVIDIA ended 3D Vision with driver **425.31** (2019-04-11); DX11 stereo was removed in Oct 2020, **DX9 is *reported* to survive on current drivers** (unconfirmed). Either way the driver-made stereo needs a 3D display + emitter or Discover-mode anaglyph glasses. ⇒ the native "Activate Stereo" item and the Ctrl+F3/F4 hotkeys **will not show a stereo picture on this machine** without a driver downgrade or driver-modding tool, unless the DX9 claim holds. Nothing on the critical path changes (the shortcut was already retired); recorded so no flat run is spent discovering it. ⚠️ Not a VR route. |
| 2026-09-02 | [The game compiles its shaders at runtime — so `d3dcompiler_43.dll` is a proxy seam](topics/2026-09-02b-the-game-compiles-its-shaders-at-runtime-so-d3dcompiler-is-a-proxy-seam.md) | ❌ dead end | **❌ Withdrawn 2026-09-03 (`/pd`, static): the shader bank ships PRE-COMPILED with `CTAB` intact — 9,971 tables in 691 layouts in the shaders/build/pc folder — and the CTAB method answered §6 the same day (`g_mViewToClip` is a standalone projection matrix at `c0` or `c192`). The game imports `D3DXCompileShaderFromFileA` from `d3dx9_43`, not `d3dcompiler_43`, and ships no shader source: the compile path is dev-only. The DirectX cabs are the stock June-2010 redist. OpenAWE/AWTools pointers still good.** Original claim: ⭐ The game ships DirectX shader-compiler cabs in `third party\directx\` and aborts with **"could not process hlsl shader"** without them — a runtime compiler implies runtime compilation, so there is **no pre-compiled cache to parse** and the `CTAB` trick that answered Alice (45,832 tables) and Enslaved (34,046) does not apply. Better route: **proxy `d3dcompiler_43.dll`** — the same technique already sanctioned for `nvapi.dll` — and every shader's HLSL source, entry point and defines are logged with constant **names**, not inferred registers. It also sits upstream of the bytecode, the eventual cleanest place for a per-eye term. Static alternative first: the archives are publicly readable (`AWTools`, `neat`), and `OpenAWE` (GPL-3.0) reimplements this engine — useful for concepts and formats, never as evidence about the retail binary. |
| 2026-09-02 | [A public cheat table locates the FOV field and a global time-scale; the Helix fix shows v1.06's shadows are FOV-dependent](topics/2026-09-02-a-public-cheat-table-locates-fov-and-time-scale-and-the-helix-fix-shows-fov-dependent-shadows.md) | ✅ incorporated | **✅ Both verified statically 2026-09-03: FOV read at `AlanWake.exe+0x3F533` (pattern unique in the exe), time-scale at `+0x29C628` (initial value 1.0) — and the accessor revealed the camera object behind ONE static global `[0x0076C5D8]` (151 callers) on a NO-ASLR image, so every static address is permanent. The post-read consumer is a linear remap (×0.4 +0.8), NOT yet shown to be the projection build.** Jim2point0's table (FRAMED) reads FOV as `fld [eax+0x214]` at `AlanWake.exe+0x3F5C3` immediately after a `call` — the camera object is in `eax` and the next `call` is the first candidate for the projection build; game speed is a float global at `+0x29D628` (`0.0001` freezes the world, a harness tool). Both in the **exe**, not the renderer DLL. Byte patterns given for the installed build. Neovad's Helix fix: v1.06 shadows are FOV-dependent (FOV 17/20 workaround), main shadow VS hash `2B37CDBA` uses `c0` — per-eye projection must reach the shadow path too. |
| 2026-09-01 | [The six NVAPI function IDs, confirmed against NVIDIA's own table](topics/2026-09-01-nvapi-function-ids-confirmed-against-nvidias-own-table.md) | ✅ incorporated | Closes the one unverified link in the modding side's `SetDriverMode` check. NVAPI dispatches by numeric ID and the shipped driver has the id→name table stripped, so the mapping had to come from NVIDIA's published `nvapi_interface.h` — where **all six IDs match**, `NvAPI_Stereo_SetDriverMode` = `0x5E8F0BEC` included, corroborated by a second independent list. Alan Wake therefore really does call `Initialize`/`CreateHandleFromIUnknown`/`Activate`/`SetSeparation` and really never asks for **Direct** mode: the driver owns both eyes, the native-stereo shortcut stays retired, and §6's rewrite is safe. |
| 2026-09-01 | [Three unrecorded switches, and a `-forcestereo` correction](topics/2026-09-01-the-three-unrecorded-switches-and-a-forcestereo-correction.md) | 🆕 new | `-forcestereo` is a **2-channel speaker-mode** flag, not stereo 3D — it sits beside `-forcesurround` in the binary and in every public list; a `Supersedes:` pointer went to `engine-research/inbox/`. The real find is **`-directaiming` / `-rigidcamera`**: Remedy's own shipped switches for 1:1 mouse control and **removing camera smoothing** — the comfort layer a VR conversion normally has to hunt down in the binary. `-nativekeys` preserves the keyboard layout on exit; `-shaders` has no public documentation at all. |
| 2026-09-01 | [3D Vision Automatic: the driver makes the eyes, not the game](topics/2026-09-01-3d-vision-automatic-the-driver-makes-the-eyes-not-the-game.md) | ✅ incorporated | Answers the modding lane's question. Neither branch it offered is right: the driver did **not** reproject, and the game's shaders did **not** apply the offset. NVIDIA's own docs — the driver appends a clip-space footer `x += Separation*(w − Convergence)` to every vertex shader and splits each draw in two. `g_vStereo_Separation_Convergence` / `Stereo Texture` is the `nvstereo.h` **correction** pattern, a consumer of driver-published values. One live thread remains: whether `Activate Stereo` calls `NvAPI_Stereo_SetDriverMode(DIRECT)`. Downgrades §6's shortcut; hands us a technique we can build ourselves. **✅ 2026-09-01: the live thread is closed.** The modding side ran the static check — `renderer_sf_Win32.dll` has **zero** callers of `NvAPI_Stereo_SetDriverMode` — and the id→name mapping that rests on is now confirmed (row above). The driver is in Automatic; §6 was rewritten accordingly. |
| 2026-08-25 | [Native -freecamera + -developermenu launch flags](topics/2026-08-25-native-freecamera-developermenu-launch-flags.md) | ✅ incorporated | The retail game ships two real Remedy-added dev tools reachable via launch options — a free camera (controller-driven) and a Developer Menu — plus a well-documented full command-line flag set. Same category as the Psychonauts precedent: a dormant official tool, not something to reverse-engineer. Factored into ENGINE-DOSSIER.md §6/§9/§10. **✅ 2026-09-01: binary-confirmed by the modding side** — both flags sit in `AlanWake.exe`'s option table immediately after the parser's own `Unknown command line option "%s"`, so they are parsed options with a rejection path, not suggestive strings. The same check recovered three switches this topic did not have; see the 2026-09-01 row above. |
| 2026-08-25 | [Native 3D Vision support + HelixMod](topics/2026-08-25-native-3d-vision-support-and-helixmod.md) | 👀 reviewed | The game shipped with real NVIDIA 3D Vision support, with later patches reportedly needing little to no third-party fixing; live Ctrl+F3/Ctrl+F4 separation hotkeys are a strong hint the per-eye offset mechanism is already reachable. Factored into ENGINE-DOSSIER.md §6/§9. |
| 2026-08-25 | [DRM history + vorpX status](topics/2026-08-25-drm-history-and-vorpx-status.md) | ✅ incorporated | Originally shipped on Games for Windows Live (undated removal, unlike Alice/PoP2008's clean dated patches); vorpX works but only confirmed in the weaker Cinema mode for the original game — a real gap vs. this portfolio's stronger fronts, recorded honestly. **Follow-up: the modding session specifically checked the installed build for GFWL/xlive artifacts and found none** — see ENGINE-DOSSIER.md §4. |

## How to add a topic

1. New file in `topics/`, named `YYYY-MM-DD-short-slug.md`.
2. One row added to the table above, newest at the top.
3. Update the status tag here as it moves through review → incorporated/dead-end (the modding side should update this when it acts on a lead, so the index reflects reality without the research side needing to poll).
